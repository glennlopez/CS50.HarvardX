#include <stdio.h>
#include <stdlib.h>

typedef struct llnode
{
    int number;
    struct llnode *next;
}
node;

//prototype
node *createNode(int num);

int main()
{



}

node *createNode(int num)
{

    return NULL;
}